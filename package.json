{
  "name": "claude-conductor",
  "version": "0.0.0",
  "description": "Disciplined multi-agent coordination for Claude Code — Apache-2.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.3.0",
    "claude-code": ">=2.1.32"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'eslint config pending — see decisions/phase-0.md entry on eslint.config.js approval' && exit 0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "bun test",
    "check-generic-paths": "./scripts/check-generic-paths.sh"
  },
  "devDependencies": {},
  "dependencies": {}
}
